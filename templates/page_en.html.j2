<!doctype html>
<html lang="en">
<head>
  <!-- Google Analytics + AdSense sont déjà dans le template FR.
       Ici, on garde un head simple pour ne rien casser pendant l'examen. -->
  <meta charset="utf-8" />
  <title>Cost of living in {{ ville }} ({{ annee }}) — rent, utilities, internet (Updated {{ date_maj }})</title>
  <meta name="description" content="Prices in {{ ville }}: 1BR rent (center & outside), utilities, internet, transport, daily basket. Updated {{ date_maj }}." />
  <link rel="canonical" href="{{ canonical_en }}" />
  <link rel="alternate" hreflang="fr" href="{{ canonical_fr }}" />
  <link rel="alternate" hreflang="en" href="{{ canonical_en }}" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="robots" content="index,follow" />
  <style>
    :root{--blue:#0a66ff;--bg:#0f1115;--card:#151923;--text:#eef2ff;--muted:#aab3c5;--border:#232838}
    *{box-sizing:border-box}
    body{font-family:system-ui,-apple-system,Segoe UI,Roboto,Ubuntu,"Helvetica Neue",Arial,sans-serif;margin:0;background:var(--bg);color:var(--text);line-height:1.55}
    main{max-width:980px;margin:0 auto;padding:10px 14px 40px}
    header{background:var(--blue);color:#fff;padding:28px 24px;border-radius:18px;margin:18px}
    h1{margin:0 0 6px 0;font-size:1.9rem}
    .badge{display:inline-block;background:rgba(255,255,255,.15);padding:6px 10px;border-radius:999px;font-size:.9rem}
    .grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(240px,1fr));gap:12px}
    .card{background:var(--card);border:1px solid var(--border);border-radius:14px;padding:14px}
    table{width:100%;border-collapse:collapse;background:var(--card);border:1px solid var(--border);border-radius:14px;overflow:hidden}
    th,td{padding:10px 12px;border-bottom:1px solid var(--border);text-align:left}
    th{background:#1a2030;color:#dfe6ff}
    nav a{display:inline-block;margin:0 8px 8px 0;color:#cfe0ff;background:#16213a;border:1px solid var(--border);padding:6px 10px;border-radius:999px;text-decoration:none}
    a{color:#cfe0ff}
  </style>
</head>
<body>
  <main>
    <header>
      <h1>Cost of living in {{ ville }} ({{ annee }})</h1>
      <div class="badge">Updated: {{ date_maj }} — Currency: {{ devise }}</div>
      <div style="margin-top:8px">
        <a href="{{ canonical_fr }}">🇫🇷 Français</a> · <strong>🇬🇧 English</strong>
      </div>
    </header>

    <section class="card">
      <h2>🏠 Housing & monthly expenses</h2>
      <div class="grid">
        <div><strong>1BR rent (city center)</strong><div>{{ rent_1br_center }} €/month</div></div>
        <div><strong>1BR rent (outside center)</strong><div>{{ rent_1br_outside }} €/month</div></div>
        <div><strong>Utilities (electricity/water/heating)</strong><div>{{ utilities_apt }} €/month</div></div>
        <div><strong>Internet (fiber/ADSL)</strong><div>{{ internet }} €/month</div></div>
        <div><strong>Monthly transport</strong><div>{{ transport_monthly }} €/month</div></div>
      </div>
    </section>

    <section class="card">
      <h2>🛒 Daily basket</h2>
      <table>
        <tr><th>Item</th><th>Price</th></tr>
        <tr><td>Inexpensive meal (1 person)</td><td>{{ meal_inexpensive }} €</td></tr>
        <tr><td>Cappuccino</td><td>{{ cappuccino }} €</td></tr>
        <tr><td>Bread (500g)</td><td>{{ bread }} €</td></tr>
        <tr><td>Eggs (12)</td><td>{{ eggs12 }} €</td></tr>
        <tr><td>Gasoline (1 L)</td><td>{{ gasoline }} €</td></tr>
      </table>
    </section>

    <section class="card">
      <h2>📌 Quick overview</h2>
      <p>Estimated monthly budget (1BR center) ≈ <strong>{{ budget_estime }} €</strong> (rent + utilities + internet + transport + 10×meals).</p>
    </section>

    <nav>
      <a href="{{ base_url }}/en/">← Cities (EN)</a>
      <a href="{{ base_url }}/">🇫🇷 Go to French</a>
    </nav>

    <footer style="opacity:.8;text-align:center;padding:24px 0">© {{ annee }} – Cost of Living</footer>
  </main>
</body>
</html>
