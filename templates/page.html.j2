<!doctype html>
<html lang="fr">
<head>
  <!-- Google AdSense Auto Ads -->
  <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-3993345981950208"
          crossorigin="anonymous"></script>

  <meta charset="utf-8" />
  <title>Coût de la vie à {{ ville }} ({{ annee }}) – loyers, charges, internet (MAJ {{ date_maj }})</title>

  <meta name="description"
        content="Prix à {{ ville }} : loyer T1 centre {{ rent_1br_center }} €, périphérie {{ rent_1br_outside }} €, internet {{ internet }} €, transport {{ transport }} €. Mise à jour {{ date_maj }}.">

  <link rel="canonical" href="{{ canonical }}" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="robots" content="index,follow" />
  <meta name="author" content="Cost of Living FR" />
  <meta name="theme-color" content="#0a66ff" />

  <!-- Open Graph (partage) -->
  <meta property="og:type" content="article" />
  <meta property="og:title" content="Coût de la vie à {{ ville }} ({{ annee }})" />
  <meta property="og:description" content="Loyers, charges, internet, panier quotidien – {{ ville }}, MAJ {{ date_maj }}" />
  <meta property="og:url" content="{{ canonical }}" />

  <!-- Données structurées (JSON-LD) -->
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "WebPage",
    "name": "Coût de la vie à {{ ville }}",
    "url": "{{ canonical }}",
    "inLanguage": "fr",
    "description": "Prix à {{ ville }} : loyers, charges, internet, transport. Mise à jour {{ date_maj }}.",
    "breadcrumb": {
      "@type": "BreadcrumbList",
      "itemListElement": [
        {"@type": "ListItem","position":1,"name":"Accueil","item":"{{ base_url }}"},
        {"@type": "ListItem","position":2,"name":"{{ ville }}","item":"{{ canonical }}"}
      ]
    }
  }
  </script>

  <style>
    body {
      font-family: system-ui, -apple-system, Segoe UI, Roboto, Ubuntu, "Helvetica Neue", Arial, sans-serif;
      max-width: 920px;
      margin: 0 auto;
      background: #111;
      color: #fff;
    }
    header {
      background: #0a66ff;
      color: #fff;
      border-radius: 14px;
      padding: 22px;
      margin: 14px 0;
    }
    h1 {
      margin: 0;
      font-size: 1.8rem;
    }
    .meta {
      opacity: .95;
      margin-top: 6px;
    }
  </style>
</head>


