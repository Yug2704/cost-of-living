<!doctype html>
<html lang="fr">
<head>
  <!-- Google AdSense Auto Ads -->
  <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-3993345981950208"
          crossorigin="anonymous"></script>

  <meta charset="utf-8" />
  <title>Coût de la vie à {{ ville }} ({{ annee }}) — loyers, charges, internet (MAJ {{ date_maj }})</title>
  <meta name="description" content="Prix moyens à {{ ville }} : loyer T1 centre & périphérie, charges, internet, transport, panier repas. Mise à jour {{ date_maj }}." />

  <!-- Si ton generate_site.py passe 'canonical_url', garde cette ligne ; si c'est 'canonical', remplace ci-dessous -->
  <link rel="canonical" href="{{ canonical_url }}" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="robots" content="index,follow" />
  <meta name="author" content="Cost of Living FR" />
  <meta name="theme-color" content="#0a66ff" />

  <!-- Open Graph -->
  <meta property="og:type" content="article" />
  <meta property="og:title" content="Coût de la vie à {{ ville }} ({{ annee }})" />
  <meta property="og:description" content="Loyers, charges, internet, panier quotidien — {{ ville }}, MAJ {{ date_maj }}." />
  <meta property="og:url" content="{{ canonical_url }}" />

  <!-- JSON-LD -->
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "WebPage",
    "name": "Coût de la vie à {{ ville }}",
    "url": "{{ canonical_url }}",
    "inLanguage": "fr",
    "description": "Prix à {{ ville }} : loyers, charges, internet, transport. Mise à jour {{ date_maj }}.",
    "breadcrumb": {
      "@type": "BreadcrumbList",
      "itemListElement": [
        { "@type":"ListItem","position":1,"name":"Accueil","item":"/" },
        { "@type":"ListItem","position":2,"name":"{{ ville }}","item":"{{ canonical_url }}" }
      ]
    }
  }
  </script>

  <style>
    :root{--blue:#0a66ff;--bg:#0f1115;--card:#151923;--text:#eef2ff;--muted:#aab3c5;--border:#232838}
    *{box-sizing:border-box}
    body{font-family:system-ui,-apple-system,Segoe UI,Roboto,Ubuntu,"Helvetica Neue",Arial,sans-serif;
         margin:0; background:var(--bg); color:var(--text); line-height:1.55}
    header{background:var(--blue); color:white; padding:28px 24px; border-radius:18px; margin:18px}
    h1{margin:0 0 6px 0; font-size:1.9rem}
    main{max-width:980px; margin:0 auto; padding:10px 14px 40px}
    .badge{display:inline-block;background:rgba(255,255,255,.15);padding:6px 10px;border-radius:999px;font-size:.9rem}
    .grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(240px,1fr));gap:12px}
    .card{background:var(--card); border:1px solid var(--border); border-radius:14px; padding:14px}
    table{width:100%;border-collapse:collapse;background:var(--card);border:1px solid var(--border);border-radius:14px;overflow:hidden}
    th,td{padding:10px 12px;border-bottom:1px solid var(--border); text-align:left}
    th{background:#1a2030;color:#dfe6ff}
    tr:last-child td{border-bottom:0}
    nav{margin-top:18px}
    nav a{display:inline-block;margin:0 8px 8px 0;color:#cfe0ff;background:#16213a;border:1px solid var(--border);padding:6px 10px;border-radius:999px;text-decoration:none}
    footer{opacity:.8; text-align:center; padding:24px 0}
    a{color:#cfe0ff}
  </style>
</head>
<body>
  <main>
    <header class="card" style="border:none">
      <h1>Coût de la vie à {{ ville }} ({{ annee }})</h1>
      <div class="badge">MAJ : {{ date_maj }} — Devise : {{ devise }}</div>
    </header>

    <section class="card">
      <h2>🏠 Loyer & dépenses</h2>
      <div class="grid">
        <div><strong>Loyer T1 centre</strong><div>{{ rent_1br_center }} € / mois</div></div>
        <div><strong>Loyer T1 périphérie</strong><div>{{ rent_1br_outside }} € / mois</div></div>
        <div><strong>Charges (élec/eau/chauffage)</strong><div>{{ utilities_apt }} € / mois</div></div>
        <div><strong>Internet</strong><div>{{ internet }} € / mois</div></div>
        <div><strong>Transport mensuel</strong><div>{{ transport_monthly }} € / mois</div></div>
      </div>
    </section>

    <section class="card">
      <h2>🛒 Panier quotidien</h2>
      <table>
        <tr><th>Article</th><th>Prix</th></tr>
        <tr><td>Repas bon marché (1 pers.)</td><td>{{ meal_inexpensive }} €</td></tr>
        <tr><td>Cappuccino</td><td>{{ cappuccino }} €</td></tr>
        <tr><td>Pain (500g)</td><td>{{ bread }} €</td></tr>
        <tr><td>Œufs (12)</td><td>{{ eggs12 }} €</td></tr>
        <tr><td>Essence (1 L)</td><td>{{ gasoline }} €</td></tr>
      </table>
    </section>

    <section class="card">
      <h2>📌 Analyse & budget</h2>
      <p>{{ texte_long }}</p>
      <ul>
        <li>Écart loyer centre vs périphérie ≈ {{ ecart_loyer_pct }} %.</li>
        <li>Budget mensuel estimatif (T1 centre) ≈ <strong>{{ budget_estime }} €</strong>/mois (loyer+charges+internet+transport+10×repas).</li>
      </ul>
    </section>

    <section class="card">
      <h2>🔗 Liens utiles (affiliation possible)</h2>
      <ul>
        <li>Banque en ligne / carte : <a href="{{ lien_aff_banque }}">ouvrir un compte</a></li>
        <li>Assurance habitation : <a href="{{ lien_aff_assurance }}">devis</a></li>
        <li>Internet fibre : <a href="{{ lien_aff_fibre }}">voir les offres</a></li>
        <li>Déménagement : <a href="{{ lien_aff_demenagement }}">comparer</a></li>
      </ul>
    </section>

    <nav>
      <a href="/cost-of-living/">Index villes</a>
      {% for link in internal_links %}
        <a href="{{ link.url }}">{{ link.label }}</a>
      {% endfor %}
    </nav>

    <footer>© {{ annee }} – Coût de la vie FR</footer>
  </main>
</body>
</html>
